#This file, .user.ini, should substitute for systemwide PHP configuration to allow running a MolProbity server when using PHP-CLI (php command line interface) for a local server. I am cautioned that it isn't working currently.

#These settings increase disk/memory limits to allow for large jobs
memory_limit = 300M
post_max_size = 500M
upload_max_filesize = 300M
