#This file, .user.ini, should substitute for systemwide PHP configuration to allow running a MolProbity server.  

#These settings increase disk/memory limits to allow for large jobs
memory_limit = 300M
post_max_size = 500M
upload_max_filesize = 300M
